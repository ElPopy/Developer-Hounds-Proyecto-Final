public with sharing class ResourceService {
  public static List<User> queryAvailableResources(
    Date projectStartDate,
    Date projectEndDate
  ) {
    List<User> availableResources = new List<User>();

    return availableResources;
  }
}
